<Alloy>
    <OptionDialog id="dialog" title="Select Image Source" destructive="2" >
        <Options>
            <Option>Use Camera</Option>
            <Option>Use Photo Album</Option>
            <Option>Cancel</Option>
        </Options>
        <!-- buttonNames only rendered on Android -->
        <ButtonNames>
            <ButtonName>Use Camera</ButtonName>
            <ButtonName>Use Photo Album</ButtonName>
            <ButtonName>Cancel</ButtonName>
        </ButtonNames>
        <!--
        Only on Android, an additional view can be added to be rendered in
        the OptionDialog, replacing any declared options. It will be
        assigned to the underlying "androidView" property of the OptionDialog.
        -->
        <View layout="horizontal" platform="android">
            <Label id="avLabel">\nChoose where to select the image from, Camera only works on device</Label>
        </View>
    </OptionDialog>
</Alloy>